{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "调试（完整运行）",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/src/123pan.py",
            "console": "integratedTerminal"
        }
    ]
}